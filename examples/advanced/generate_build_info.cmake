set(OUTPUT_FILE "${OUTPUT_FILE}")

if(GIT_COMMIT_HASH STREQUAL "")
    set(GIT_COMMIT_HASH "unknown")
endif()

file(WRITE "${OUTPUT_FILE}" "// Auto-generated by CMake - do not edit\n")
file(APPEND "${OUTPUT_FILE}" "#pragma once\n")
file(APPEND "${OUTPUT_FILE}" "\n")
file(APPEND "${OUTPUT_FILE}" "#include <string_view>\n")
file(APPEND "${OUTPUT_FILE}" "\n")
file(APPEND "${OUTPUT_FILE}" "namespace build_info {\n")
file(APPEND "${OUTPUT_FILE}" "\n")
file(APPEND "${OUTPUT_FILE}" "constexpr std::string_view version = \"${PROJECT_VERSION}\";\n")
file(APPEND "${OUTPUT_FILE}" "constexpr std::string_view build_type = \"${BUILD_TYPE}\";\n")
file(APPEND "${OUTPUT_FILE}" "constexpr std::string_view git_commit = \"${GIT_COMMIT_HASH}\";\n")
file(APPEND "${OUTPUT_FILE}" "\n")
file(APPEND "${OUTPUT_FILE}" "}\n")
